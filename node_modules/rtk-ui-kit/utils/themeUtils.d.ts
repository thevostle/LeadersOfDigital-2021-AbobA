import { BASE_COLORS } from '../constants/components';
import { ITheme } from '../types/theme';
import { SystemColorsType } from '../types/base';
export declare const getColor: (alpha: number, color: string) => string;
export declare const defaultAlpha = 1;
export declare const getThemedSystemColors: (type: SystemColorsType | undefined, theme: ITheme, alpha?: number) => string;
export declare const getThemedBaseColors: (color: BASE_COLORS | undefined, theme: ITheme, alpha?: number) => string;
export declare const generateThemeTransition: (types: Array<string>, themeTransition: string) => string;
