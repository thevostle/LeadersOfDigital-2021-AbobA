import { CSSProperties } from 'react';
import { BaseColorsType } from '../../../types/base';
import { ITheme } from '../../../types/theme';
export interface IRadioButton {
    /** Задает основной цвет для radio
     *  @default "primary2" */
    color?: BaseColorsType;
    /** Задает атрибут name для компонента */
    name: string;
    /** Задает value */
    value?: string;
    /** Устанавливает атрибут disabled */
    disabled?: boolean;
    /** Callback функция, вызываемая при изменении значения */
    onChange?: (val: string) => void;
    /** Устанавливает дополнительные props */
    inputProps?: any;
    /** Задает дополнительные классы */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
}
export declare type StyledRadioButtonType = Omit<IRadioButton, 'name' | 'color'> & {
    color?: string;
    theme: ITheme;
};
