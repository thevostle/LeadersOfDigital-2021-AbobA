import { CSSProperties } from 'react';
import { ITheme } from 'types/theme';
import { TagsViewsType, TagsSizesType } from '../constants';
import { ShapesType } from '../../../types/base';
export interface ITagMore {
    /** Задает внешний вид
     *  @default "primary" */
    view?: TagsViewsType;
    /** Задает размер
     *  @default "medium" */
    size?: TagsSizesType;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Устанавливает дополнительные классы для элемента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    onClick: () => void;
}
export declare type StyledTagMoreType = Omit<ITagMore, 'className' | 'onClick'> & {
    theme: ITheme;
};
