import { CSSProperties, ChangeEvent, KeyboardEvent } from 'react';
import { ITheme } from 'types/theme';
import { TagsSizesType } from '../constants';
import { ShapesType } from '../../../types/base';
export interface ITagInput {
    value: string;
    /** Задает размер
     *  @default "medium" */
    size: TagsSizesType;
    /** Задает скругление */
    shape: ShapesType;
    /** Устанавливает дополнительные классы для элемента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Callback функция, вызываемая при изменении значения */
    onChange: (event: ChangeEvent<HTMLInputElement>) => void;
    /** Callback функция, вызываемая при потере фокуса */
    onBlur: () => void;
    /** Callback функция, вызываемая при нажатии на клавишу клавиатуры */
    onKeyPress: (event: KeyboardEvent) => void;
}
export declare type StyledTagInputType = Omit<ITagInput, 'onChange' | 'onBlur' | 'onKeyPress'> & {
    theme: ITheme;
};
