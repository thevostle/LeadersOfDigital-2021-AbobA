import { CSSProperties } from 'react';
import { ITheme } from 'types/theme';
import { TAG_SIZES, TAG_VIEWS } from '../constants';
import { ShapesType } from '../../../types/base';
export declare type TagsSizesType = keyof typeof TAG_SIZES;
export declare type TagsViewsType = keyof typeof TAG_VIEWS;
export interface ITagGroupItem {
    value: string;
    key: string | number;
}
export interface ITagGroup {
    /** Задает внешний вид
     *  @default "primary" */
    view?: TagsViewsType;
    /** Задает размер
     *  @default "medium" */
    size?: TagsSizesType;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Список тагов
     * @param value - отображаемое значение
     * @param key - уникальный ключ в списке тагов
     */
    tags: ITagGroupItem[];
    /** Список disabled тагов
     * @param value - отображаемое значение
     * @param key - уникальный ключ в списке тагов
     */
    disabledTags?: ITagGroupItem[];
    /** Позволяет удалять таги
     *  @default false */
    closable?: boolean;
    /** Позволяет добавлять таги
     *  @default false */
    editable?: boolean;
    /** Задает текст для кнопки добавления нового тага */
    editLabel?: string;
    /** Устанавливает ограничение по количеству символов для каждого тага */
    maxSymbols?: number;
    /** Устанавливает дополнительные классы для элемента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Устанавливает дополнительные классы для элемента */
    inputClassName?: string;
    /** Задает дополнительные стили для компонента */
    inputStyle?: CSSProperties;
    /** Устанавливает дополнительные классы для элемента */
    itemClassName?: string;
    /** Задает дополнительные стили для компонента */
    itemStyle?: CSSProperties;
    /** Устанавливает дополнительные классы для элемента */
    moreClassName?: string;
    /** Задает дополнительные стили для компонента */
    moreStyle?: CSSProperties;
    /** Callback функция, вызываемая при добавлении тага */
    onAdd?: (addedTag: ITagGroupItem) => void;
    /** Callback функция, вызываемая при удалении тага */
    onRemove?: (removedTag: ITagGroupItem) => void;
}
export declare type StyledTagType = Omit<ITagGroup, 'tags' | 'onAdd' | 'onRemove'> & {
    theme: ITheme;
};
