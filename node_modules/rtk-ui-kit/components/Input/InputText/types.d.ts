import { ITheme } from '../../../types/theme';
import { ShapesType } from '../../../types/base';
export declare type HintPropsType = {
    disabled: boolean;
    error: boolean;
    theme: ITheme;
    shape: ShapesType;
};
