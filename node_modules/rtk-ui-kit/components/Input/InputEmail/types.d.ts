import { IInputProps } from '../types';
export interface IInputEmailProps extends IInputProps {
    /** Текст ошибки валидации
     * @default "Некорректный формат почты"
     */
    defaultValidationText?: string;
}
