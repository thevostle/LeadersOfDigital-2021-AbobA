import { ReactNode, CSSProperties } from 'react';
import { ShapesType } from '../../types/base';
import { HINT_POSITIONS, IMAGE_POSITIONS, TEXT_ALIGNS, TOOLTIP_PLACEMENTS, TOOLTIP_TRIGGERS } from './constants';
import { ITheme } from '../../types/theme';
export interface ITooltipProps {
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Задает заголовок */
    header?: string;
    /** Задает основной текст */
    text?: ReactNode;
    /** Задает выравнивание основного текста
     *  @default "left" */
    textAlign?: TextAlignsType;
    /** Задает описание к изображению */
    hint?: string;
    /** Задает положение описания к изображению относительно изображения
     * @default "bottom" */
    hintSide?: HintPositionsType;
    /** Задает путь до картинки */
    image?: string;
    /** Задает положение изображения относительно основного текста
     * @default "bottom" */
    imageSide?: ImagePositionsType;
    /** Задает отображение кнопки "Закрыть"
     *  @default false */
    hideCloseButton?: boolean;
    /** Задает событие триггер для отображения
     *  @default "click" */
    trigger?: TooltipTriggersType;
    /** Задает расположение для отображения
     *  @default "auto" */
    placement?: PlacementsType;
    /** Задает дополнительные классы для компонента, к примеру для переопределения максимальной ширины = 246px */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    children?: ReactNode;
}
export interface ITooltipStyle {
    theme: ITheme;
    shape?: ShapesType;
    isOpen?: boolean;
    closeButton?: boolean;
    placement: PlacementsType;
    currentTargetRect: IElementClientRect;
}
export interface ITooltipTextStyle {
    textAlign?: TextAlignsType;
}
export interface IElementClientRect {
    width: number;
    height: number;
    top: number;
    bottom: number;
    left: number;
    right: number;
}
export declare type TooltipTriggersType = keyof typeof TOOLTIP_TRIGGERS;
export declare type HintPositionsType = keyof typeof HINT_POSITIONS;
export declare type ImagePositionsType = keyof typeof IMAGE_POSITIONS;
export declare type PlacementsType = keyof typeof TOOLTIP_PLACEMENTS;
export declare type TextAlignsType = keyof typeof TEXT_ALIGNS;
