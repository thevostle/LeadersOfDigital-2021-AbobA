import { ReactNode, ElementType, CSSProperties } from 'react';
import { ITheme } from '../../../types/theme';
import { BaseColorsType, ShapesType } from '../../../types/base';
export declare type IFloatingActionMenuItem = {
    /** Задает основной цвет */
    color?: BaseColorsType;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Задает дополнительные классы для компонента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Задает тег для пункта (React.ElementType)
     *  @default "a" */
    tag?: ElementType;
    /** Callback функция, вызываемая при клике */
    onItemClick?(event: MouseEvent): void;
    /** Позволяет задать дополнительные параметры для компонента */
    [x: string]: any;
} & ({
    text?: string;
    icon: ReactNode;
} | {
    text: string;
    icon?: ReactNode;
});
export declare type StyledIFloatingActionMenuItemType = Pick<IFloatingActionMenuItem, 'shape'> & {
    color?: string;
    text?: string;
    theme: ITheme;
};
