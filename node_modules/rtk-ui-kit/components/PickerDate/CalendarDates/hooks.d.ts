import { ICalendarDates, CalendarDateType } from './types';
export declare const useCalendarDates: ({ date, activeDate, secondDate, isRange, onSelect, firstDayIndex, minDate, maxDate, disabledDates, }: ICalendarDates) => {
    activeValue: Date;
    secondValue: Date;
    dates: CalendarDateType[];
    handleClick: (day: CalendarDateType) => (e: any) => void;
    handleMouseEnter: (value: Date) => () => void;
    handeMouseLeave: () => void;
};
