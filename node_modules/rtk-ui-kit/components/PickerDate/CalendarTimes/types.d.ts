import { CSSProperties } from 'react';
import { ShapesType, BaseColorsType } from '../../../types/base';
import { ITheme } from '../../../types/theme';
import { TIME_STATUS } from './constants';
export interface ICalendarTimes {
    date?: Date;
    shape?: ShapesType;
    color?: BaseColorsType;
    format?: string;
    firstTime?: Date;
    secondTime?: Date;
    isRange?: boolean;
    onSelect?: (activeDate: Date, secondDate?: Date) => void;
    interval?: number;
    className?: string;
    style?: CSSProperties;
}
export declare type CalendarTimeType = {
    time: Date;
    status: TIME_STATUS;
};
export interface StyledCalendarTimeItemType {
    theme: ITheme;
    status: TIME_STATUS;
    shape: ShapesType;
    color: string;
}
