import { MutableRefObject } from 'react';
import { ICalendarTimes } from './types';
export declare const useCalendarTimes: ({ date, firstTime, secondTime, isRange, onSelect, interval, format, }: ICalendarTimes, ref: MutableRefObject<HTMLDivElement | null>) => {
    activeValue: Date;
    secondValue: Date;
    times: import("./types").CalendarTimeType[];
    handleClick: (time: Date) => (e: any) => void;
    handleMouseEnter: (value: Date) => () => void;
    handeMouseLeave: () => void;
};
