import { MouseEvent } from 'react';
import { ShapesType } from '../../types/base';
import { ITheme } from '../../types/theme';
export declare type StyledPickerDateItemType = {
    theme: ITheme;
    shape: ShapesType;
    active?: boolean;
    onClick?: (e: MouseEvent<HTMLElement>) => void;
};
export interface StyledPickerDateSelectebleItemType extends StyledPickerDateItemType {
    baseColor: string;
}
