import { ShapesType, BaseColorsType } from '../../../types/base';
export interface ICalendarYears {
    shape?: ShapesType;
    color?: BaseColorsType;
    date: Date;
    minYear?: number;
    maxYear?: number;
    onSelect: (year: number) => void;
}
