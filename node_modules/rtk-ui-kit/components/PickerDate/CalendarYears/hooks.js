'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true,
});

exports.useCalendarYears = void 0;

var react_1 = require('react');

var useCalendarYears = function useCalendarYears(ref, minYear, maxYear) {
  react_1.useEffect(
    function () {
      if (!ref.current) {
        return;
      }

      ref.current.scrollIntoView({
        inline: 'center',
      });
    },
    [ref]
  );
  var years = Array(maxYear - minYear + 1)
    .fill(minYear)
    .map(function (year, index) {
      return year + index;
    });
  return {
    years: years,
  };
};

exports.useCalendarYears = useCalendarYears;
