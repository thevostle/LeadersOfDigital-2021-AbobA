import { CSSProperties } from 'react';
import { ITheme } from '../../types/theme';
import { OverlayViewsType } from './constants';
export interface IOverlay {
    /** Ярлык видимости компонента
     *  @default false
     */
    show?: boolean;
    /** Устанавливает тип компонента
     *  @default "standart"
     */
    view?: OverlayViewsType;
    /** Задает id для компонента */
    id?: string;
    /** Задает дополнительные классы для компонента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Задает использование в Portal
     *  @default true */
    useInPortal?: boolean;
    /** Callback функция, вызываемая при клике */
    onClick?(): void;
}
export declare type StyledOverlayType = Pick<IOverlay, 'view'> & {
    theme: ITheme;
};
