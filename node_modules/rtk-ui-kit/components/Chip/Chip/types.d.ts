import { MouseEvent } from 'react';
import { BaseColorsType, ShapesType } from '../../../types/base';
import { ChipViewsType } from '../constants';
import { ITheme } from '../../../types/theme';
import { IButtonBaseProps } from '../../Base/BaseButton/types';
export interface IChip extends Omit<IButtonBaseProps, 'onClick' | 'type'> {
    /** Задает основной цвет
     *  @default "primary2" */
    color?: BaseColorsType;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Задает внешний вид
     *  @default "primary" */
    view?: ChipViewsType;
    /** Задает значение счетчика */
    amount?: number;
    /** Ярлык активного чипа */
    selected?: boolean;
    /** Callback функция, вызываемая при клике */
    onClick?: (event: MouseEvent<HTMLButtonElement>) => void;
}
export declare type StyledChipType = Pick<IChip, 'shape' | 'view' | 'selected' | 'disabled'> & {
    color?: string;
    theme: ITheme;
};
