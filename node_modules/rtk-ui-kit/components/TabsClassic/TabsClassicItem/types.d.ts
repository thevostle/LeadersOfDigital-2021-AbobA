import { ReactNode, MouseEvent, CSSProperties } from 'react';
import { ITheme } from '../../../types/theme';
import { TabsSizesType, TabsIconPositionsType } from '../constants';
export interface ITabsClassicItem {
    /** Индекс текущей кнопки таба */
    index: string;
    /** Текст кнопки таба */
    label: string;
    size?: TabsSizesType;
    /** Иконка для кнопки таба */
    icon?: ReactNode;
    /** Задает расположение иконки относительно текста
     *  @default "left" */
    iconPosition?: TabsIconPositionsType;
    /** Устанавливает атрибут disabled для таба */
    disabled?: boolean;
    /** Задает дополнительные классы */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Callback функция, вызываемая при изменении значения */
    onClick?: (event: MouseEvent<HTMLButtonElement>) => void;
}
export declare type StyledTabsClassicItemType = Pick<ITabsClassicItem, 'size' | 'iconPosition' | 'disabled'> & {
    scrollable?: boolean;
    accentColor?: string;
    isActive?: boolean;
    theme: ITheme;
};
