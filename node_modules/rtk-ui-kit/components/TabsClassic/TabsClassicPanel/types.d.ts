import { ReactNode, CSSProperties } from 'react';
import { ITheme } from '../../../types/theme';
export interface ITabsClassicPanel {
    /** Индекс видимой панели таба */
    index: string;
    /** Индекс текущей панели таба
     *  Если value равен index - панель видима
     */
    value: string;
    children?: ReactNode;
    /** Задает дополнительные классы */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
}
export declare type StyledTabsClassicPanelType = {
    theme: ITheme;
};
