import { CSSProperties, ReactElement } from 'react';
import { BaseColorsType } from '../../../types/base';
import { ITheme } from '../../../types/theme';
import { TabsSizesType } from '../constants';
export interface ITabsClassicGroup {
    /** Задает основной цвет для активной кнопки группы
     *  @default "primary2" */
    accentColor?: BaseColorsType;
    /** Задает размер
     *  @default "medium" */
    size?: TabsSizesType;
    /** Задает выбор активного таба.
     *  Рекомендуется по дефолту выбирать первый tab */
    value: string;
    /** Задает disabled для всей группы */
    disabledGroup?: boolean;
    /** Задает полосу внизу панели
     *  @default true
     */
    classicBar?: boolean;
    /** Задает прокрутку для табов.
     *  @param false Табы устанавливаются на всю ширину, если не помещаются - обрезаются многоточием
     *  @param true Табы занимают только ширину табов, оставляя пространство, если не помещаются - появляется прокрутка
     *  @default false
     */
    scrollable?: boolean;
    /** Задает id для элемента */
    id?: string;
    /** Задает дополнительные классы для элемента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Callback функция, вызываемая при изменении значения */
    onChange?: (index: string) => void;
    children: ReactElement[];
}
export declare type StyledTabsClassicGroupType = Omit<ITabsClassicGroup, 'value' | 'children'> & {
    theme: ITheme;
};
