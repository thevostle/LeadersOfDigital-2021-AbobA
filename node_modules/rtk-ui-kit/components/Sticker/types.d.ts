import { CSSProperties } from 'react';
import { SystemColorsType, BaseColorsType, ShapesType } from '../../types/base';
import { ITheme } from '../../types/theme';
export interface IStickerProps {
    /** Устанавливает тип отображаемого компонента */
    type: SystemColorsType | BaseColorsType;
    /** Устанавливает атрибут disabled */
    disabled?: boolean;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Устанавливает дополнительные классы для элемента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
}
export declare type StickerContainerPropsType = Pick<IStickerProps, 'type' | 'disabled'> & {
    shape: ShapesType;
    theme: ITheme;
};
