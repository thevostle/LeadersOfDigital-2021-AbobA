import { MouseEvent, CSSProperties } from 'react';
import { BUTTON_TYPES } from '../../../constants/components';
export declare type ButtonTypesType = keyof typeof BUTTON_TYPES;
export interface IButtonBaseProps {
    /** Устанавливает тип кнопки
     *  @default "button" */
    type?: ButtonTypesType;
    /** Устанавливает атрибут disabled */
    disabled?: boolean;
    /** Задает id для компонента */
    id?: string;
    /** Задает дополнительные классы для компонента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
    /** Задает атрибут name для компонента */
    name?: string;
    /** Callback функция, вызываемая при клике */
    onClick?(event: MouseEvent<HTMLButtonElement>): void;
}
