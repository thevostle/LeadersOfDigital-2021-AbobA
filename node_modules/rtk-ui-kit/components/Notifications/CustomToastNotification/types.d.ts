import { ReactNode, ReactElement } from 'react';
import { SystemColorsType, BaseColorsType } from '../../../types/base';
import { NotificationPositionType, INotification } from '../types';
export interface ICustomToastNotification extends INotification {
    /** Задает иконку */
    icon?: ReactNode;
    /** Задает цвет подложки для иконки */
    iconBgColor?: SystemColorsType | BaseColorsType;
    /** Задает время в милисекундах, через которое уведомление скроется */
    timeout?: number;
    /** Задает кнопки уведомления */
    actionButtons?: (id: string) => ReactElement[];
    /** Задает расположение уведомления */
    position?: NotificationPositionType;
}
export interface ICustomToastNotificationWithIsCustom extends ICustomToastNotification {
    isCustom?: boolean;
}
