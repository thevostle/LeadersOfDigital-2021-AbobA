import { CSSProperties } from 'react';
import { ShapesType } from '../../types/base';
import { NOTIFICATION_POSITION } from './constants';
export interface INotification {
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Задает заголовок уведомления */
    title?: string;
    /** Задает подзаголовок уведомления */
    subtitle?: string;
    /** Callback функция, вызываемая при появлении уведомления
     * @returns {undefined}
     */
    onOpen?: () => void;
    /** Callback функция, вызываемая при закрытии уведомления
     * @returns {undefined}
     */
    onClose?: () => void;
    /** Прячет кнопку "Закрыть"
     *  @default false */
    hideCloseButton?: boolean;
    /** Задает id для компонента */
    id?: string;
    /** Задает дополнительные классы для компонента */
    className?: string;
    /** Задает дополнительные стили для компонента */
    style?: CSSProperties;
}
export declare type NotificationPositionType = keyof typeof NOTIFICATION_POSITION;
export interface INotificationButton {
    text: string;
    action: (id?: string) => void;
}
export interface INotificationButtonWithId extends INotificationButton {
    id: number;
}
