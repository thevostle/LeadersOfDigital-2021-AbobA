import { ReactNode } from 'react';
import { BaseColorsType, ShapesType, SizesType, IconPositionsType } from '../../../types/base';
import { ITheme } from '../../../types/theme';
import { BUTTON_VIEWS } from '../../../constants/components';
import { IButtonBaseProps } from '../../Base/BaseButton/types';
export declare type ButtonViewsType = keyof typeof BUTTON_VIEWS;
export interface IButtonProps extends IButtonBaseProps {
    /** Задает основной цвет для кнопки
     *  @default "primary2" */
    color?: BaseColorsType;
    /** Задает размер
     *  @default "medium" */
    size?: SizesType;
    /** Задает скругление, значение по умолчанию берется из темы */
    shape?: ShapesType;
    /** Задает внешний вид
     *  @default "primary" */
    view?: ButtonViewsType;
    /** Иконка для кнопки */
    icon?: ReactNode;
    /** Задает расположение иконки относительно текста
     *  @default "left" */
    iconPosition?: IconPositionsType;
}
export declare type StyledButtonPropsType = Omit<IButtonProps, 'color'> & {
    color: string;
    colorhover: string;
    coloractive: string;
    theme: ITheme;
};
