import { ShapesType } from '../../../types/base';
declare const CloseButton: (props: {
    onClick: () => void;
    shape?: ShapesType;
    className?: string;
}) => JSX.Element;
export default CloseButton;
