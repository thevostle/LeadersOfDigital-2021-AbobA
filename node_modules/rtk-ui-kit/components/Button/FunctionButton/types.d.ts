import { ReactNode } from 'react';
import { BaseColorsType, ViewsType, IconPositionsType } from '../../../types/base';
import { ITheme } from '../../../types/theme';
import { IButtonBaseProps } from '../../Base/BaseButton/types';
export interface IFunctionButtonProps extends IButtonBaseProps {
    /** Задает основной цвет для кнопки
     *  @default "primary2" */
    color?: BaseColorsType;
    /** Задает внешний вид
     *  @default "primary" */
    view?: ViewsType;
    /** Иконка для кнопки */
    icon?: ReactNode;
    /** Задает расположение иконки относительно текста
     *  @default "left" */
    iconPosition?: IconPositionsType;
}
export declare type StyledFunctionButtonPropsType = Omit<IFunctionButtonProps, 'color'> & {
    color: string;
    theme: ITheme;
};
