import React, { ReactNode } from 'react';
import { IMultiselectOption, IMultiselectOptionExtended } from './types';
export declare const useIsMount: () => boolean;
export declare const useMultiselect: (inputOptions: IMultiselectOption[], checkIcon: ReactNode, defaultValue?: IMultiselectOption[]) => {
    options: IMultiselectOptionExtended[];
    dropdownItems: {
        key: string | number;
        value: string;
        hint: string;
        icon: React.ReactNode;
    }[];
    selectedOptions: {
        key: string | number;
        value: string;
    }[];
    updateOptions: React.Dispatch<React.SetStateAction<IMultiselectOptionExtended[]>>;
    updateMultiselectInputs: (changedOptions: IMultiselectOption[], valueToSet?: IMultiselectOption[]) => void;
};
